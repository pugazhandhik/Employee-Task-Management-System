#!/bin/bash
# Build script for Render

# Install dependencies
pip install -r requirements.txt

# Optional: Run seed data (comment out if you don't want to reset data on every deploy)
# python seed_data.py
